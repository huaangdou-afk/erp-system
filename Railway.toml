[railway]
  builder = "nixpacks"
  language = "node"

[deploy]
  preDeployCommand = "npm run build"
  startCommand = "node src/server/index.js"

[disk]
  name = "erp-backend-db"
  mountPath = "/app/src/db"
