name: Deploy ERP System

on:
  push:
    branches: [main]
  workflow_dispatch:

# Railway 已配置 GitHub 自动部署，push 代码即自动触发后端+前端部署
# Railway.toml 指定了后端 Root Directory: src/server
# 前端 Root Directory: src/client
# 无需 GitHub Actions 调用 API

